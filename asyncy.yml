commands:
  template:
    help: |
      Evaluate a Handlebars template
    args:
      - name: Path
        type: file
        required: true
        help: |
          Path to template
      - name: expressions
        type: string
        required: false
        help: |
          Handlebar expressions

volume: true

env:
  TEMPLATE_PATH:
  EXPRESSIONS:
